<form action='/accounts/sign_up' method="get">
<label>Secret number</label>
<input type="text" name="secret_number">
<button>Submit</button>

</form>

<% if sign(params[:secret_number])%>
<div >
<h3 class="text-lg bg-green-500 text-white h-12 text-center font-bold py-2">Votre paiement est bien réussi</h3>
<p class="text-left text-lg w-1/2 mx-auto mt-16 font-medium text-gray-900  ">Pour accéder à votre Dashboard veuillez créer un compte.<br> Pour cela veuillez utilisez le meme email que pour le paiement.
<br> Une fois votre compte crée rendez vous dans le menu Dashboard pour conitnuer la création de votre site</p>
<h2 class="text-center text-gray-900 font-bold text-xl mt-12">Créez votre compte</h2>

<div class="flex  space-x-64 pl-48 ">

          <div class=" w-1/3 px-20 mt-24 ">

                <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
                  <%= render "devise/shared/error_messages", resource: resource %>

                  <div class=" ">
                         <div class=""><%= f.label :email %><br /></div>
                         <div class="border-4 border-indigo-200 rounded-lg "> <%= f.email_field :email, placeholder: "votre email" %></div>
                  </div>

                  <div class="field">
                    <%= f.label :password, 'Mot de passe' %>
                    <% if @minimum_password_length %>
                    <em>(<%= @minimum_password_length %> characters minimum)</em>
                    <% end %><br />
                      <div class="border-4 border-indigo-200 rounded-lg "><%= f.password_field :password, autocomplete: "new-password", placeholder:"Votre mot de passe" %></div>
                  </div>

                  <div class="field">
                    <%= f.label :password_confirmation,"Confirmez votre mot de passe" %><br />
                     <div class="border-4 border-indigo-200 rounded-lg "><%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder:"Confirmez votre mot de passe" %></div>
                  </div>

                  <div class="actions">
                    <%= f.submit "Sign up" %>
                  </div>
                <% end %>

                <%= render "devise/shared/links" %>
          </div>
          <div class=" mt-12"><%= image_tag "Finance app_Isometric.svg", height: 300 %></div>

</div>
</div>
<%else%>
<h2>Vous n'avez pas le bon secret</h2>
<%end%>
 